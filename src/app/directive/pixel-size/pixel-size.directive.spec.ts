import { PixelSizeDirective } from './pixel-size.directive';

describe('PixelSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PixelSizeDirective();
    expect(directive).toBeTruthy();
  });
});
